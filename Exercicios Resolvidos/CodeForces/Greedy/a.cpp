#include <iostream>
#include <vector>
using namespace std;

typedef int levi_munhoz;
typedef vector<int> bianchi;

#define glauco(a) cout << "Danado: " << a << "\n";
#define pietro_desenvolvedor(b) cin >> b;
#define bia(isaque) for(int i=0; i<isaque; i++)
#define lucimar(bianchi, corki_barao_vermelho) bianchi.push_back(corki_barao_vermelho);

int main() {  //define de io
	levi_munhoz valor;
	bianchi danado(10);
	pietro_desenvolvedor(valor);
	glauco(valor)
	bia(valor) glauco(valor)
	lucimar(danado, 5)
	glauco(danado[10])
	
}

